<!-- ---
// src/pages/projects/index.astro
import Layout from "@/layouts/Layout.astro";
import ProjectsGrid from "@/components/main/ProjectsGrid";
import { projects } from "@/data/projects";

// Get current path
const currentPath = Astro.url.pathname;
import { getAllPhotographyPosts } from "@/lib/sanity";
const posts = await getAllPhotographyPosts();
console.log("posts:", posts);
// Get unique categories from projects
const categories = [
    "All",
    ...new Set(projects.map((project) => project.category)),
];

// Page metadata
const pageTitle = "My Projects";
const pageDescription =
    "Explore My full portfolio of creative work across various disciplines";
---

<Layout>
    <ProjectsGrid
        title={pageTitle}
        subtitle={pageDescription}
        allProjects={posts}
        categories={categories}
        defaultTab="All"
        currentPath={currentPath}
        client:load
    />
</Layout> -->
